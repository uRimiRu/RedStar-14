<!--
SPDX-FileCopyrightText: 2022 Nemanja <98561806+EmoGarbage404@users.noreply.github.com>
SPDX-FileCopyrightText: 2025 Aiden <28298836+Aidenkrz@users.noreply.github.com>

SPDX-License-Identifier: MIT
-->

<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'mech-menu-title'}"
                      MinSize="390 560"
                      SetSize="390 560">
    <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
        <BoxContainer Margin="10 10 10 10" Orientation="Horizontal" HorizontalExpand="True">
            <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
                    <ProgressBar Name="IntegrityDisplayBar"
                                 HorizontalExpand="True"
                                 SetHeight="25"
                                 MaxValue="1"
                                 Value="0">
                        <Label Name="IntegrityDisplay"
                               HorizontalAlignment="Left"
                               Margin="5 0 0 0"
                               VerticalAlignment="Center" />
                    </ProgressBar>
                </BoxContainer>
                <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="0 5 0 0">
                    <ProgressBar Name="EnergyDisplayBar"
                                 HorizontalExpand="True"
                                 SetHeight="25"
                                 MaxValue="1"
                                 Value="0">
                        <Label Name="EnergyDisplay"
                               Margin="5 0 0 0"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"></Label>
                    </ProgressBar>
                </BoxContainer>
                <Label Name="SlotDisplay"
                       HorizontalAlignment="Left"
                       Access="Public"
                       HorizontalExpand="True" />
                <!-- RS14-start -->
                <Label Name="ModuleSlotDisplay"
                       HorizontalAlignment="Left"
                       Access="Public"
                       HorizontalExpand="True" />
                <!-- RS14-end -->
            </BoxContainer>
            <SpriteView Name="MechView"
                        Margin="10 0 0 0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        OverrideDirection="South"
                        SetSize="64 64"
                        MaxSize="64 64"
                        Scale="2 2">
            </SpriteView>
        </BoxContainer>
        <!-- RS14-start -->
        <PanelContainer Name="LockControlsContainer" Margin="10 0 10 10">
            <BoxContainer Orientation="Vertical" Margin="6 6 6 6">
                <Label Name="LockStatusLabel" StyleClasses="LabelHeading" />
                <GridContainer Columns="2" HSeparationOverride="8" VSeparationOverride="4">
                    <BoxContainer Orientation="Vertical">
                        <Label Text="{Loc 'mech-lock-dna-label'}" />
                        <BoxContainer Orientation="Horizontal">
                            <Button Name="DnaLockRegisterButton" Text="{Loc 'mech-lock-register'}" />
                            <Button Name="DnaLockToggleButton" />
                            <Button Name="DnaLockResetButton" Text="{Loc 'mech-lock-reset'}" />
                        </BoxContainer>
                        <Label Name="DnaLockInfoLabel" ClipText="True" />
                    </BoxContainer>
                    <BoxContainer Orientation="Vertical">
                        <Label Text="{Loc 'mech-lock-card-label'}" />
                        <BoxContainer Orientation="Horizontal">
                            <Button Name="CardLockRegisterButton" Text="{Loc 'mech-lock-register'}" />
                            <Button Name="CardLockToggleButton" />
                            <Button Name="CardLockResetButton" Text="{Loc 'mech-lock-reset'}" />
                        </BoxContainer>
                        <Label Name="CardLockInfoLabel" ClipText="True" />
                    </BoxContainer>
                </GridContainer>
            </BoxContainer>
        </PanelContainer>
        <!-- RS14-end -->
        <BoxContainer VerticalExpand="True" Margin="10 0 10 10" Orientation="Vertical">
            <Label Text="{Loc 'mech-equipment-section'}" />
            <PanelContainer VerticalExpand="True" MinSize="0 140">
                <PanelContainer.PanelOverride>
                    <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                </PanelContainer.PanelOverride>
                <ScrollContainer
                    HScrollEnabled="False"
                    HorizontalExpand="True"
                    MinSize="100 140"
                    SizeFlagsStretchRatio="2"
                    VerticalExpand="True">
                    <BoxContainer
                        Name="EquipmentControlContainer"
                        MinSize="100 140"
                        Orientation="Vertical"
                        SizeFlagsStretchRatio="2"
                        VerticalExpand="True">
                    </BoxContainer>
                </ScrollContainer>
            </PanelContainer>
            <!-- RS14-start -->
            <Label Text="{Loc 'mech-module-section'}" Margin="0 6 0 0" />
            <PanelContainer VerticalExpand="True" MinSize="0 120">
                <PanelContainer.PanelOverride>
                    <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                </PanelContainer.PanelOverride>
                <ScrollContainer
                    HScrollEnabled="False"
                    HorizontalExpand="True"
                    MinSize="100 120"
                    SizeFlagsStretchRatio="2"
                    VerticalExpand="True">
                    <BoxContainer
                        Name="ModuleControlContainer"
                        MinSize="100 120"
                        Orientation="Vertical"
                        SizeFlagsStretchRatio="2"
                        VerticalExpand="True">
                    </BoxContainer>
                </ScrollContainer>
            </PanelContainer>
            <!-- RS14-end -->
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
